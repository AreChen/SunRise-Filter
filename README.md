# SunRise Filter v1.0.0

Chinese hard-coded EasternSunLAN quality-of-life loot filter based on Sully's QOL v0.3.6.

## Auto Update URL

```text
https://raw.githubusercontent.com/AreChen/SunRise-Filter/refs/heads/main/SunRise-Filter.lua
```

## Files

- `SunRise-Filter.lua` - D2RLAN loot filter source.
- `sounds/` - optional custom drop sounds used by the filter.

## Notes

This version intentionally hard-codes Chinese display strings and keeps `language = "enUS"` so the D2RLAN launcher does not strip multilingual `enUS` / `zhCN` tables while generating `lootfilter_config.lua`.

Original filter attribution: Sully's QOL v0.3.6.
